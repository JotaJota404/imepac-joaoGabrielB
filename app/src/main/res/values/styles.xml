<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Container de formulários (card branco -> card escuro com borda âmbar) -->
    <style name="ContainerComponents">
        <item name="android:background">@drawable/container_components</item>
        <item name="android:padding">24dp</item>
    </style>

    <!-- Campos de texto (escuro com borda âmbar, texto claro) -->
    <style name="Edit_Text">
        <item name="android:background">@drawable/edit_text</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">@color/texto_principal</item>
        <item name="android:textColorHint">@color/texto_secundario</item>
    </style>

    <!-- Botão primário (âmbar dourado) -->
    <style name="button">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/fundo_escuro</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- Botão de perigo (vermelho — Sair, Excluir) -->
    <style name="button_perigo">
        <item name="android:background">@drawable/button_perigo</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- Avatar circular para ShapeableImageView — perfil do usuário -->
    <style name="AvatarCircle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>